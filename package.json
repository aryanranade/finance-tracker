{
  "name": "finance-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "AI-Powered Personal Finance Tracker — monorepo runner",
  "scripts": {
    "dev": "concurrently -k -n server,client -c blue,magenta \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "start": "npm --prefix server start",
    "build": "npm --prefix client run build",
    "seed": "npm --prefix server run seed",
    "setup": "node scripts/setup.js && npm run install:all",
    "install:all": "npm install && npm --prefix server install && npm --prefix client install"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
